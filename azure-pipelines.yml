# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool: Default

steps:

- script: |
    mkdir New-pipeline
  displayName: 'Nuevo directorio'

- script: |
    npm install -g npm-cli-login
    npm-cli-login -u ci24 -p $controles$ -e jequesada@gmail.com
    npm install ci
    npm run build
  displayName: 'npm install and build modules'

- script: |
    node backup.js 
  displayName: 'node backup.js'